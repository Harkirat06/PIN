import React, { useContext } from "react";
import { Container, Row, Button, Col } from 'react-bootstrap';

function PagarNuestro({ context }) {
    return (
        <Container>
            <h1>Buy our product second hand!</h1>
        </Container>
    );
}

export default PagarNuestro;